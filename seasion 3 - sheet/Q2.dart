// ignore_for_file: unused_local_variable

void main() {
  int age = 24;
  double height = 1.78;
  num score = 96;
  age.toString() + " years";
  print(age);
  num total = age + height;
  print("Total = $total");
  print ("comparison operators : score >+90 && height > 1.7 is ${score > 90 && height < 2.0}");
}
